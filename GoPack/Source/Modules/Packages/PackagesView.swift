import SwiftUI

struct PackagesView: View {
    var body: some View {
        Text("Packages View")
            .toolbar(.hidden)
    }
}

struct PackagesView_Previews: PreviewProvider {
    static var previews: some View {
        PackagesView()
    }
}
